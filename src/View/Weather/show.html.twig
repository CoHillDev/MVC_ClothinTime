{% extends 'Layout/column.html.twig' %}

{% block content %}

{% if weatherData %}
    <h1>Here is the weather in {{ weatherData.name }}, {{ weatherData.sys.country }}</h1>

    <img class="memes" src="{{ '/assets/images/weather/' ~ weatherData.description }}">
    <img class="memes" src="{{ weatherData.weather[0].icon_url }}">

    <p>Description : {{ weatherData.weather[0].description }}</p> 
    <p>Temperature : {{ weatherData.main.temp|round }}°C</p>

    {% else %}

{% include 'Weather/_form.html.twig' %}

{% endif %}

{% if error %}
    <p>{{ error }}</p>
{% endif %}

{% endblock %}